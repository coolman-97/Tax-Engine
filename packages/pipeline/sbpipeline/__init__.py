"""Documents in, attested facts out.

The pipeline's job is to turn a household's tax returns into the data the
engine runs on - without ever letting a model produce a number that nobody can
check. See docs/adr/0006-two-pass-extraction.md.
"""

__version__ = "0.1.0"
